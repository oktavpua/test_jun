Задание №1. Подготовка тестового окружения.

# Обновляем информацию о пакетах
apt update

#Устанавливаем nginx
apt install nginx

# Проверяем статус сервиса
systemctl status nginx

# Убедимся что приложение работает и слушает порт
ss -ntulp | grep nginx

# Проверим какой ip у локальной машины (192.168.1.117)
ip a

# Открываем в браузере адрес: 192.168.1.117 и видим приветственную страницу nginx (Welcome to nginx!)
# Или локально утилитой curl: curl http://localhost
